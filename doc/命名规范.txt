分类的命名方法：使用单个字母+"_"为前缀

1.布局（grid）（.g_）;模块（module）（.m_）;元件（unit）（.u_）;

2.按钮（button）（.u_btn_）;图标（icon）（.u_ico_）;

3.字体大小（size）（.u_fs）;

4.大（big）(_big);中（middle）(_mid);大（small）(_sma);不可点（disable）(_dis);